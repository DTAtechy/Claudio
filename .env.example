# Postgres (matches docker-compose.yml; the host port is 5433 to avoid conflicts)
DATABASE_URL="postgresql://claudio:claudio_dev_password@localhost:5433/claudio?schema=public"

# Server
PORT=4000
JWT_SECRET="change-me-in-production-use-a-long-random-string"
STORAGE_DIR="./storage"
CORS_ORIGIN="http://localhost:5173"

# Desktop client (Electron renderer reads VITE_*)
VITE_API_URL="http://localhost:4000"
VITE_WS_URL="ws://localhost:4000/ws"
